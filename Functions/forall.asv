function varargout = forall(varargin)
nOutputs=nargout;
varargout=cell(1,nOutputs);
nInputs=nargin;
if nInputs==2 && nOutputs==1
    a=size(varargin(1),1);
    b=size(varargin(1),2);
    gigi=varargin(1);
    for i=1:a
        for j=1:b
            varargout{1}(i,j)=get(gigi{i,j},varargin{2});
        end
    end
elseif nInputs==3
    
end
end

